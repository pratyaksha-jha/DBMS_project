Document the work done or to be done in here

nirf-insights-project/
│
├── backend/                 # Python (Flask/FastAPI, Web Scraper, SQLite)
│   ├── data/                # Raw scraped files (if any)
│   ├── database/            # SQLite setup and queries
│   ├── scraper/             # Beautiful Soup scripts
│   ├── api/                 # API routes serving data to React
│   ├── requirements.txt     # Python dependencies
│   └── main.py              # Entry point to run your backend server
│
├── frontend/                # React (Vite/CRA, Map, Charts)
│   ├── public/              # Static assets (images, raw map JSONs)
│   ├── src/                 # React source code
│   ├── package.json         # Node.js dependencies
│   └── vite.config.js       # Vite configuration (if using Vite)
│
└── README.md                # Project documentation


1. The Backend (Python)
I highly recommend using FastAPI or Flask to create a lightweight API that connects your SQLite database to your React frontend.
backend/main.py: The central file that starts your server. It connects your API routes and initializes the database connection.
backend/scraper/nirf_scraper.py: Your Beautiful Soup logic goes here. This script will make HTTP requests to the NIRF website, parse the HTML tables, and extract parameters like Teaching, Learning & Resources (TLR), Research (RPC), etc.
backend/database/models.py: This file handles your database schema. If you use an ORM like SQLAlchemy (recommended), you will define your tables here (e.g., Institute, Ranking, Parameters).
backend/database/db_utils.py: Helper functions to insert your scraped data into SQLite and execute SQL queries to fetch data for the frontend.
backend/api/routes.py: Your API endpoints. For example:
    /api/institutes -> Returns a list of all institutes.
    /api/institutes/{id}/parameters -> Returns the specific NIRF parameters for the graphs.
    /api/map-data -> Returns state-wise data for the India map.

backend/database/nirf_data.db: This will be your actual SQLite database file (auto-generated once you run your DB creation script).

2. The Frontend (React)
Your React folder will handle the UI, state management, and visualizations.

frontend/src/App.jsx: The main React component that holds your routing logic (e.g., navigating from the Home page to an Institute Details page).
frontend/src/services/api.js: Centralized file for all your Axios or Fetch calls to communicate with your Python backend.
frontend/src/pages/:
  Dashboard.jsx: The main landing page.
  InstituteDetail.jsx: A page dedicated to a single institute showing its specific graphs.

frontend/src/components/: This is where your reusable UI widgets live.
  IndiaMap.jsx: The map component. You can use libraries like react-simple-maps combined with a TopoJSON file of India.
  ParameterChart.jsx: Your graphing component. Libraries like Recharts or Chart.js are perfect for displaying NIRF parameters (Bar charts for TLR vs. RPC scores, line charts for historical rankings).
  InstituteCard.jsx: A simple card displaying basic institute info (Name, State, Overall Rank) used in lists.

frontend/public/india-states.json: If you use an interactive map, you will likely need a GeoJSON or TopoJSON file of India's map, which you should store in the public folder.

27 March:
Scraped all data from NIRF website for 5 years 2021-2025, 4 domains - Overall,Engineering,Management,Research using Beautiful Soup.
Data written to csv file nirf.csv and database file nirf.db table rankings.
Table contains 15 columns- id,name,tlr,rpc,go,oi,perception,city,state,total,rank,year,domain,latitude,longitude
Latitude, Longitude collected through OpenStreetMap
Database file and python script scraper.py prent is scraper folder of backend
How to run:
cd backend
pip install -r requirements.txt
cd scraper
python scraper.py
Can access database from the database file too without running the script


27 March:
Designed and implemented structured database for NIRF data.

Converted raw scraped data (nirf.db) into a clean normalized database (clean_nirf.db).

Created 3 tables:
- institutes (id, name, city, state, latitude, longitude)
- rankings (id, year, domain, rank, score)
- parameters (id, year, domain, tlr, rpc, go, oi, pr)

Used Python (sqlite3) to:
- Read raw data from scraper database
- Remove redundancy using mapping (institute_map)
- Insert data into respective tables
- Maintain primary keys and foreign key relationships

Implemented transformation script:
backend/database/transform.py

How to run:
cd backend/database
python transform.py

Output:
Creates clean database file:
backend/data/clean_nirf.db


Also implemented SQL queries for analysis:
- Top 5 institutes (by rank)
- Top institutes by RPC
- New institutes (year-wise comparison)
- Rank improvement analysis
- State-wise institute distribution

File:
backend/database/queries.py

How to run:
cd backend/database
python queries.py

28 March
frontend map part complete
topojson file -> frontend/public/india.json
location fetched from -> backend/database/nirf.db/rankings
in order to run:
cd backend
uvicorn main:app --reload

in new terminal:
cd frontend
npm run dev

8 March:
pdf scraping part complete
pdf scraping is in general slow, but to optimize the code a little,
we used ThreadPoolExecutor library:
-it basically inserts threads of five rows, instead of one row at a time
Also the research domain does not habe pdfs.


9 April:

Integrated budget data[nirf_full.db] into the main database[clean_nirf.db].
Added new table:
- budget (id, nirf_year, domain, fin_year, library, equipment, workshops, studios, capital_assets, salaries, maintenance, seminars, research_amount, consultancy_amount)
Script:
backend/database/budget_transform.py

How to run:
cd backend/scraper
python budget.py

cd ../database
python budget_transform.py

Output:
Adds budget table to clean_nirf.db

12 march
Completed institute level analysis part - added rank graph, parameter scores for all the 5 years along withtheir definition and pie cahrt depicting the expenditures of the institute budget for the last three financial years
